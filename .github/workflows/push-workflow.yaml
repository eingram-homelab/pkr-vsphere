name: Push Workflow

on:
  push:
    branches-ignore:
      - main
    paths:
      - '**.pkr.hcl'

jobs:
  validate:
    uses: eingram-homelab/reusable_workflows/.github/workflows/packer-validate.yaml@main
    name: Validate Packer Project
    secrets:
      VAULT_TOKEN: ${{ secrets.VAULT_TOKEN }}
